# UseCase Guide

Use `UseCase` for direct delivery, `ChainUseCase` for dependent comparison flow, `SequenceUseCase` for ordered multi-step execution, and guards to reject invalid dispatch before work begins.
